apiVersion: v1
kind: Service
metadata:
  name: mysql
  labels: {app: mysql}
spec:
  clusterIP: None
  selector: {app: mysql}
  ports:
    - port: 3306
      name: mysql
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  labels: {app: mysql}
spec:
  type: ClusterIP
  selector: {app: mysql}
  ports:
    - port: 3306
      targetPort: 3306
      name: mysql
